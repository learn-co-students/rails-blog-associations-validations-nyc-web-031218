<p id="notice"><%= notice %></p>

<p>
  <h3>Name:</h3>
  <%= @post.name %>
  <label>Tags:</label>
  <% @post.tags.each{|tag| %>
    <%= tag.name %> <br>
    <% } %>
</p>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
